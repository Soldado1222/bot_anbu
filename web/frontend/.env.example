# Frontend Configuration
# Les variables d'environnement sont gérées via le proxy Vite
# Voir vite.config.ts pour la configuration du proxy

# Backend API URL (automatiquement proxifié par Vite)
# En développement : http://localhost:3001
# En production : remplacez par votre URL de backend
VITE_API_URL=http://localhost:3001
